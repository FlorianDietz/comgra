
# These are defined in a separate file and then imported in utilities.py,
# so that it is possible to change them before importing utilities.py
# This is necessary for decorators to work correctly.
DEBUG_MODE = False
PRINT_EACH_TIME = False
